# ─── Talent Mates Concierge (web widget) ───────────────────────────
# Both vars must be NEXT_PUBLIC_* so they reach the client bundle.
# The anon key is safe to expose — RLS is enabled on the Supabase
# project and there are no anon policies, so reads/writes from the
# browser hit the Edge Function only.

NEXT_PUBLIC_CONCIERGE_ENDPOINT=https://kqvkyxwwflnturtwxlit.supabase.co/functions/v1/talent-mates-concierge
NEXT_PUBLIC_CONCIERGE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
